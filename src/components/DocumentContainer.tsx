import { css } from "styled-system/css"
import { styled } from "styled-system/jsx"
import { container } from "styled-system/patterns"

export const DocumentContainer = styled("main", container.raw())
